root = "."
tmp_dir = "tmp"

[build]
  cmd = "go generate ./... && go build -buildvcs=false -o ./tmp/server ."
  bin = "./tmp/server"
  include_ext = ["go"]
  exclude_dir = ["tmp", "minecraft-server", "docs"]
  delay = 1000

[misc]
  clean_on_exit = true
