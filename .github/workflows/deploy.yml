name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to homelab
    runs-on: ubuntu-latest
    steps:
      - name: Install cloudflared
        run: |
          curl -fsSL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -o cloudflared
          chmod +x cloudflared
          sudo mv cloudflared /usr/local/bin/

      - name: Deploy via SSH
        env:
          SSH_KEY: ${{ secrets.HOMELAB_SSH_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_KEY" > ~/.ssh/key
          chmod 600 ~/.ssh/key
          ssh -o StrictHostKeyChecking=no \
              -o ProxyCommand="cloudflared access ssh --hostname ${{ secrets.HOMELAB_HOST }}" \
              -i ~/.ssh/key \
              ${{ secrets.HOMELAB_USER }}@${{ secrets.HOMELAB_HOST }} \
              "cd /home/lomokwa/homelab/mc-manager/mc-manager-server && git pull origin main && docker compose up --build -d"
